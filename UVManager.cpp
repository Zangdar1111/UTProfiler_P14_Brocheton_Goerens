#include "includes.h"
/*
void UVManager::addUV(string code, string t, ...)
{
    if (TabUV.exist(code))
        editUV(...);
    else
        UV(code);
        TabUV.add(code);
        editUV(string code, ...);
}
void UVManager::deleteUV(string code)
{
    tabUV.delete(code);
}
*/
